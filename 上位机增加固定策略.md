# 本次上位机升级说明

## 目标

在原有 ONNX 策略之外，支持从文件逐帧发送固定的 12 关节目标角；同时修复关节角读取工具在等待首帧时可能无法启动通信的问题。STM32 固件本次未修改。

## 一、固定策略

- 新增 `humanoid_jetson_deploy/fixed_joint_policy.py`，读取并校验 `joint_frames_v1` JSON。
- `humanoid_jetson_deploy/main.py` 新增 `--fixed-policy`。指定该选项时不需要 `--model`；原 ONNX 模式继续用 `--model`。
- 固定策略每 20 ms 取一帧，复用现有的关节限位、目标限速、相对实测角窗口、串口发送和位置日志。每次发送后等待 STM32 回传**新的状态序号**；回包超时、故障或编码器/IMU 无效会停机并发送去使能帧。最后一帧完成后自动停机，不循环。
- `policy_runner.py` 和 `serial_link.py` 的依赖改为在实际使用时加载，因此只跑固定策略不需要安装 `onnxruntime`，离线读取/测试也不需要先安装 `pyserial`；连接串口时仍需要 `pyserial`。

格式模板：`humanoid_jetson_deploy/examples/fixed_joint_frames.json`。字段说明：

| 字段 | 要求 |
|---|---|
| `format` | 固定为 `joint_frames_v1` |
| `hz` | 固定为 `50` |
| `joint_names` | 与 `config.JOINT_NAMES` 完全相同、顺序相同 |
| `frames` | 非空数组；每项有 12 个有限弧度值，且在配置的关节安全限位内 |

关节顺序是右腿 6 个、左腿 6 个，与原 ONNX 上位机逻辑一致。本次没有在上位机重新排列左右腿；下位机现有的历史通道转换保持原样。想保持姿态多个周期，就重复同一帧。

在 `humanoid_jetson_deploy/` 目录运行：

```bash
# 先检查发送链路，电机不使能
python main.py --fixed-policy examples/fixed_joint_frames.json --no-plot

# 确认通道、方向和限位后，在机器人可靠支撑下使能
python main.py --fixed-policy examples/fixed_joint_frames.json --enable-motors --no-plot
```

## 二、关节角读取启动修复

原 `read_joint_angles.py` 的顺序是先等首个 STM32 状态帧，再启动保活线程；保活线程也只有见到状态帧才发送命令。在“下位机收到命令才返回状态”的情况下，这会互相等待。

现在命令行工具和 `joint_angle_gui.py` 在等首帧期间会周期发送去使能探测帧：目标角全零、`kp_scale=0`、`kd_scale=0`、`command_flags=0`。收到首帧以后，命令行工具仍按原逻辑回显当前实测角度做保活。`--no-keepalive` 不发送首帧探测，也不发送命令行工具的后续保活帧。首帧等待失败时，清理流程不会再尝试 `join` 尚未启动的记录线程。

当前仓库的 STM32 代码在电机和 IMU 数据满足就绪条件时也会主动上传状态；如果真机仍无回包，除了启动握手，还应检查电机/IMU 就绪标志、USB CDC 串口和固件版本。

读取命令：

```bash
cd humanoid_jetson_deploy
python tools/read_joint_angles.py --port /dev/ttyACM0
```

## 验证与范围

### 真机读取补充（2026-09-25）

真机测试中，STM32 状态序号持续增长，但手动转动多个关节时角度仍接近启动值。这说明 USB 状态包在刷新，不能单凭序号或 `STATE_ENCODERS_VALID` 判断电机 CAN 反馈也在刷新。实际烧录的下位机固件与本仓库版本不同，用户已将实际电机 KP/KD 设为 0。

读取工具新增显式 `--zero-gain-readback`。在**已确认实际烧录固件全部电机 KP=0、KD=0、前馈力矩为 0**时，使用 `python tools/read_joint_angles.py --port /dev/ttyACM0 --zero-gain-readback`。该模式首帧后请求电机使能，使 STM32 持续发出电机 CAN 控制帧、获得新的状态反馈；每帧仍携带最新实测角，退出时去使能。普通模式保持不使能。仓库中的 STM32 固件仍使用固定非零增益，不能直接用它运行此选项。尚需真机验证转动关节后读数是否变化；若仍冻结，必须检查实际固件的 CAN 反馈链路。

- 固定策略、原 walking 模式、读取工具和 GUI 的针对性测试：62 个通过。
- 全量运行 125 个测试，其中 `one-foot` 模式有 2 个已有的时间边界断言失败；本次未修改该模式的时间调度。
- 尚未连接实体 STM32/电机进行硬件验证。首次使能前应先用不使能模式核对状态、关节顺序和日志。
